\chapter{Additional Tables and Hyperparameters}
\label{app:hyperparams}

This appendix collects the exact hyperparameters, architectural sizes, augmentation probabilities, dataset filters and inference defaults used throughout the project. It is reference material: the body chapters cite the relevant table when an exact configuration is needed to reproduce a result. Unless otherwise stated, the values reported here are the defaults of the \texttt{Config} dataclass in \texttt{src/CRNN\_CTC/config.py}, which is serialised into every checkpoint at training time.

\section{Optical Music Recognition CRNN}

\Cref{tab:hyperparams-omr} lists the final training hyperparameters of the music-recognition CRNN; \Cref{tab:arch-omr} reports its architectural sizes.

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} L l @{}}
\toprule
\textbf{Hyperparameter} & \textbf{Value} \\
\midrule
Optimizer                       & AdamW \\
Peak learning rate              & \num{1e-3} \\
Weight decay                    & \num{1e-4} \\
LR schedule                     & OneCycleLR (cosine) \\
Warm-up fraction                & 0.08 of total steps \\
Annealing end LR                & peak / \num{1e4} \\
Maximum epochs                  & 60 \\
Early-stopping patience         & 12 (epochs on val SER plateau) \\
Train batch size                & 16 \\
Validation batch size           & 16 \\
Gradient clipping (max norm)    & 5.0 \\
Mixed precision (AMP)           & enabled on CUDA devices \\
DataLoader workers              & 10 \\
\texttt{persistent\_workers}    & True (when \texttt{num\_workers} > 0) \\
\texttt{pin\_memory}            & True \\
Image height (\texttt{img\_height})       & 128 px \\
Maximum image width (\texttt{max\_image\_width}) & 2048 px \\
CTC loss                        & \texttt{nn.CTCLoss(blank=0, zero\_infinity=True)} \\
CTC blank index                 & 0 (hardcoded; see \Cref{ch:impl}) \\
Random seed                     & 42 \\
\bottomrule
\end{tabularx}
\caption{Final hyperparameters of the music-recognition CRNN, as serialised in every checkpoint.}
\label{tab:hyperparams-omr}
\end{table}

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} L l @{}}
\toprule
\textbf{Component} & \textbf{Value} \\
\midrule
CNN backbone (default)              & ResNet18 (modified, asymmetric strides) \\
CNN backbone (legacy)               & 5-block VGG-style \\
CNN output channels (ResNet18)      & 512 \\
CNN output channels (VGG)           & 256 (\texttt{cnn\_out\_channels}) \\
CNN dropout                         & 0.25 (VGG only) \\
Width compression factor            & $W / 4$ \\
Height collapse                     & to 1 via \texttt{AdaptiveAvgPool2d} \\
BiLSTM layers                       & 2 (stacked, bidirectional) \\
BiLSTM hidden size per direction    & 256 \\
BiLSTM combined output size         & 512 \\
Inter-layer LSTM dropout            & 0.3 \\
Final linear projection             & 512 to \texttt{vocab\_size} \\
Vocabulary size                     & \num{80} symbols (\num{77} content tokens plus blank, pad, unk) \\
Total parameter count (ResNet18, vocab\_size=80) & \num{14365200} (\textasciitilde\num{14.4} M) \\
\bottomrule
\end{tabularx}
\caption{Architecture summary of the music-recognition CRNN.}
\label{tab:arch-omr}
\end{table}

\section{Offline scan-simulation augmentation}

\Cref{tab:aug} lists the augmentation operations applied by \texttt{src/data\_processing/augment\_scanned.py} to produce the \texttt{scanned/} copies of every clean LilyJAZZ render. The albumentations block is applied first; the post-processing steps are applied afterwards in the order shown. An additional lightweight online jitter (brightness $\pm 5 \%$, contrast bias $\pm 3 \%$, Gaussian noise $\sigma \approx 0.01$, $\pm 2$ px horizontal shift) is applied during training only with probability $\texttt{online\_aug\_prob} = 0.5$.

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} l L c @{}}
\toprule
\textbf{Transform} & \textbf{Parameters} & \textbf{Prob.} \\
\midrule
\multicolumn{3}{@{}l}{\textit{Albumentations block (geometric + photometric)}} \\
\texttt{ElasticTransform}            & $\alpha = 22$, $\sigma = 4.5$            & 0.82 \\
\texttt{GridDistortion}              & 5 steps, distort $\pm 0.08$              & 0.72 \\
\texttt{OpticalDistortion}           & distort limit 0.12                       & 0.38 \\
\texttt{Affine}                      & rot.\ $\pm 4^{\circ}$, shear $\pm 1.2^{\circ}$, scale 0.97--1.03, transl.\ $\pm 1.5\%$ & 0.85 \\
\texttt{GaussianBlur}                & $\sigma \in [0.2, 1.1]$                  & 0.62 \\
\texttt{Sharpen}                     & $\alpha \in [0.15, 0.45]$, lightness $\in [0.85, 1.0]$ & 0.48 \\
\texttt{RandomToneCurve}             & scale 0.12                               & 0.72 \\
\texttt{GaussNoise}                  & std range $[0.012, 0.045]$               & 0.78 \\
\texttt{RandomBrightnessContrast}    & brightness $\pm 0.06$, contrast $[0.03, 0.18]$ & 0.70 \\
\texttt{ImageCompression}            & JPEG quality $[72, 92]$                  & 0.35 \\
\midrule
\multicolumn{3}{@{}l}{\textit{Post-processing steps (applied in order)}} \\
\texttt{dilate\_ink}        & 0, 1, or 2 morphological erosion iterations (weights 0.42\,/\,0.48\,/\,0.10) & 1.0 \\
\texttt{vary\_staff\_line\_thickness} & $\pm 1$ px morphological tweak on detected staff lines  & 0.40 \\
\texttt{remap\_tones}       & linear remap to paper range $[28, 245]$  & 1.0 \\
\texttt{add\_vignette}      & radial darkening, strength $\approx 0.15$ jittered $\pm 40\%$ & 0.50\textsuperscript{*} \\
\texttt{add\_uneven\_illumination} & directional gradient, max strength 0.25, side $\in \{\text{left, right, top}\}$ & 0.35\textsuperscript{*} \\
\texttt{add\_halftone\_lines} & period $\in [4, 9]$ rows, strength $\in [0.03, 0.07]$ & 0.25 \\
\bottomrule
\end{tabularx}
\caption{Parameters of the offline scan-simulation augmentation pipeline.
\textsuperscript{*}Vignette and uneven illumination are mutually
exclusive: a single roll selects one, the other, or neither.}
\label{tab:aug}
\end{table}

\section{Training-time dataset filters}

The three filter flags in \Cref{tab:filters} realise the lead-sheet domain contract described in \Cref{ch:design}. They are applied at dataset construction time inside \texttt{OMRDataset}, not during data generation: the renderer and the converter write every sample to disk, and the filters decide which of those samples participate in training.

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} l c L @{}}
\toprule
\textbf{Flag} & \textbf{Default} & \textbf{Effect} \\
\midrule
\texttt{filter\_multi\_staff}         & True & Drops samples whose original PNG height exceeds \texttt{max\_source\_height} (180\,px). Removes LilyPond renders that wrapped onto two staves. \\
\texttt{filter\_non\_leadsheet\_clef} & True & Keeps only \texttt{clef:G2} (treble). Drops alto, tenor, bass, and French-violin clefs inherited from orchestral PrIMuS sources. \\
\texttt{filter\_unusual\_time}        & True & Keeps only the eight common jazz meters: $\{4/4, 3/4, 2/4, 2/2, 6/8, 6/4, 5/4, 12/8\}$. \\
\bottomrule
\end{tabularx}
\caption{Domain filters applied at training-set construction.}
\label{tab:filters}
\end{table}

The allowed time-signature set must remain in sync between \texttt{src/CRNN\_CTC/dataset.py} (\texttt{\_COMMON\_TIME\_SIGS}) and \texttt{src/omr\_pipeline/grammar\_fix.py} (\texttt{\_COMMON\_TIME\_SIGS}); the grammar fixer rejects, at inference, any prediction outside this set.

\section{Chord OCR CRNN}

The chord-symbol recognition stage uses a separate, smaller CRNN trained on synthetic LilyJAZZ strips and fine-tuned on hand-labelled Real Book strips. Its defaults are summarised in \Cref{tab:hyperparams-chord} and live in the \texttt{ChordConfig} dataclass at \texttt{src/CRNN\_CTC/chord\_train.py}; the fine-tuning stage at \texttt{src/CRNN\_CTC/chord\_finetune.py} overrides a subset of these.

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} L l l @{}}
\toprule
\textbf{Hyperparameter} & \textbf{From scratch} & \textbf{Fine-tune} \\
\midrule
Optimizer                         & AdamW               & AdamW \\
Peak learning rate                & \num{1e-3}          & \num{2e-4} \\
Weight decay                      & \num{1e-4}          & \num{1e-4} \\
LR schedule                       & OneCycleLR (cosine) & OneCycleLR (cosine) \\
Warm-up fraction                  & 0.05                & 0.05 \\
Maximum epochs                    & 30                  & 20 \\
Early-stopping patience           & 8                   & 6 \\
Batch size                        & 32                  & 32 \\
Gradient clipping (max norm)      & 5.0                 & 5.0 \\
DataLoader workers                & 6                   & 6 \\
Image height                      & 64\,px              & 64\,px \\
Maximum image width               & 2048\,px            & 2048\,px \\
Backbone                          & ResNet18            & inherited from checkpoint \\
BiLSTM layers                     & 2                   & 2 \\
BiLSTM hidden size                & 192 per direction   & 192 per direction \\
LSTM inter-layer dropout          & 0.2                 & 0.2 \\
CNN dropout                       & 0.15                & 0.15 \\
Vocabulary size                   & 29 (26 char + 3 special) & 29 \\
Real-to-synthetic sampling weight & N/A                 & real 1.0 / synth 0.4 \\
\bottomrule
\end{tabularx}
\caption{Hyperparameters of the chord-OCR CRNN at training and fine-tuning.}
\label{tab:hyperparams-chord}
\end{table}

\section{Inference-time settings}

\Cref{tab:inference} reports the defaults of the inference pipeline. Most of these are read from environment variables at start-up rather than from \texttt{Config}, because they control behaviour of a checkpoint that is already fixed.

\begin{table}[H]
\centering\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.30}
\begin{tabularx}{\textwidth}{@{} p{0.40\textwidth} L @{}}
\toprule
\textbf{Setting} & \textbf{Default} \\
\midrule
PDF rasterisation DPI (\texttt{OMR\_PDF\_DPI})   & 300, clamped to $[72, 600]$ \\
CTC decoder                                      & greedy (argmax then collapse-and-strip-blanks) \\
Beam-search width (\texttt{OMR\_BEAM\_WIDTH})    & 1 (greedy). Width~5 yields $\leq 1$ edit per 1000 SER improvement at ${\sim}6\times$ decode cost. \\
Multi-page handling                              & only page 0 of multi-page PDFs is processed \\
Tiling mode (\texttt{OMR\_ENABLE\_TILING})       & disabled (legacy mode) \\
Chord checkpoint (\texttt{OMR\_CHORD\_CHECKPOINT}) & \texttt{models/chord/latest/best\_model.pt} \\
\midrule
\multicolumn{2}{l}{\textit{Staff detection morphology (\texttt{src/omr\_pipeline/staff\_detect.py})}} \\
Text-line removal kernel                         & $\max(25,\, W / 6) \times 1$ rectangle, \texttt{MORPH\_OPEN}, 1 iteration \\
Horizontal staff-line kernel                     & $hh \times 1$ rectangle, where $hh$ is the per-page line-length estimate \\
\midrule
\multicolumn{2}{l}{\textit{Staff rejection thresholds (\texttt{models/staff\_reject/thresholds.json})}} \\
\texttt{min\_line\_span\_frac}                   & 0.25 \\
\texttt{max\_spacing\_cov}                       & 0.10 \\
\texttt{min\_interline\_ink\_frac}               & 0.10 \\
\texttt{max\_text\_area\_frac}                   & 0.10 \\
\texttt{min\_mean\_logprob} (CTC gate)           & $-0.05$ (checkpoint-dependent) \\
\texttt{confident\_override\_logprob}            & $-0.05$ (checkpoint-dependent) \\
\bottomrule
\end{tabularx}
\caption{Default inference-time settings of the OMR pipeline.}
\label{tab:inference}
\end{table}

The two log-probability thresholds in \Cref{tab:inference} are calibrated against the log-probability distribution of the active CRNN checkpoint and must be recomputed after every retraining via \texttt{poetry run python src/cli.py calibrate-reject \ldots}; the values shown reflect the current \texttt{models/latest/best\_model.pt}.

\section{Technology Stack}

\Cref{tab:tech-stack} lists the principal libraries used in each layer
of the system and the version pins recorded in \code{pyproject.toml}.
Poetry's \code{poetry.lock} fixes every transitive dependency, so the
environment is reproducible across machines with \code{poetry install}.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.35}
\begin{tabularx}{\textwidth}{@{} l l L @{}}
\toprule
\textbf{Layer} & \textbf{Library / version} & \textbf{Role} \\
\midrule
Deep learning            & PyTorch $\geq 2.10$~\cite{paszkeImperativeStyleHighPerformance2019} & Model, loss, optimiser, mixed-precision training. \\ \tfgrowsep
CNN backbone             & \code{torchvision} $\geq 0.25$~\cite{torchvision2016} & ResNet18 module definition. \\ \tfgrowsep
Image processing         & OpenCV $\geq 4.13$~\cite{bradskiOpenCVLibrary2000} & Binarisation, deskewing, morphological staff detection. \\ \tfgrowsep
PDF rasterisation        & PyMuPDF (\code{fitz}) $\geq 1.27$~\cite{artifexPyMuPDF} & Render PDF pages to grayscale arrays at configurable DPI. \\ \tfgrowsep
Music rendering          & LilyPond $\geq 2.25$ + LilyJAZZ~\cite{nienhuysLilyPondSystem2003,hammerleOpenLilyPondFontsLilyJAZZ} & Generate training PNGs from PrIMuS semantic files. \\ \tfgrowsep
Data augmentation        & \code{albumentations} $\geq 2.0$\,\cite{AlbumentationsFastFlexible} & Scan-simulation transform pipeline. \\ \tfgrowsep
Numerics                 & NumPy $\geq 2.4$~\cite{harrisArrayProgramming2020} & Tensor / array interop. \\ \tfgrowsep
Text-region OCR          & EasyOCR $\geq 1.7$~\cite{jaidedaiEasyOCR} & Pre-CRNN staff-reject gate (text-density signal). \\ \tfgrowsep
Web service              & FastAPI $\geq 0.115$ + Uvicorn $\geq 0.32$~\cite{ramirezFastAPI} & REST API wrapping the inference pipeline. \\ \tfgrowsep
Package manager          & Poetry $\geq 2.0$ & Dependency isolation, deterministic environment. \\
\bottomrule
\end{tabularx}
\caption{Principal libraries and version pins (\Cref{sec:impl-stack}).}
\label{tab:tech-stack}
\end{table}

\section{Command-Line Interface}

\Cref{tab:cli-subcommands} lists the thirteen subcommands of
\code{src/cli.py}.
Every subcommand prints self-contained help text when invoked with
\code{--help}.

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.35}
\begin{tabularx}{\textwidth}{@{} l L @{}}
\toprule
\textbf{Subcommand} & \textbf{Purpose} \\
\midrule
\code{render}                  & Render PrIMuS \code{.semantic} files to LilyJAZZ PNGs. \\ \tfgrowsep
\code{convert}                 & Convert PrIMuS \code{.semantic} files to LMX labels. \\ \tfgrowsep
\code{augment}                 & Apply scan-simulation augmentation to clean PNGs. \\ \tfgrowsep
\code{vocab}                   & Build the LMX vocabulary file from a corpus. \\ \tfgrowsep
\code{pipeline}                & Run \code{render} + \code{convert} + twins + \code{augment} + \code{vocab} (\code{--force-all} rebuilds all). \\ \tfgrowsep
\code{train}                   & Train the CRNN on a prepared dataset. \\ \tfgrowsep
\code{evaluate}                & Evaluate a checkpoint and report SER. \\ \tfgrowsep
\code{evaluate-ab}             & Compare beam widths on the same checkpoint. \\ \tfgrowsep
\code{api}                     & Launch the FastAPI web service. \\ \tfgrowsep
\code{pipeline-train}          & Run the full data pipeline then immediately start training. \\ \tfgrowsep
\code{generate-header-templates} & Prerender clef/key/time header templates for continuation-staff injection. \\ \tfgrowsep
\code{harvest-reject-fixtures} & Cut staff-strip fixtures from real PDFs for reject-gate calibration. \\ \tfgrowsep
\code{calibrate-reject}        & Sweep reject-gate thresholds on labelled fixtures and emit \code{thresholds.json}. \\
\bottomrule
\end{tabularx}
\caption{CLI subcommands exposed by \code{src/cli.py} (\Cref{sec:impl-cli}).}
\label{tab:cli-subcommands}
\end{table}

\section{Project Task Summary}
\label{app:task-summary}

\Cref{tab:task-summary} lists the 31 tasks of the project plan
with their effort estimates, dependency chain, and principal
resources; it is the source for the 509-hour human-resources total
used in the budget of \Cref{sec:mgmt-budget}.

\begin{table}[H]
    \centering
    \caption{Task summary with effort estimates, dependencies, and
    principal resources.}
    \label{tab:task-summary}
    \renewcommand{\arraystretch}{1.05}
    \scriptsize
    \begin{tabularx}{\linewidth}{c >{\raggedright}p{4.2cm} r >{\raggedright\arraybackslash}p{2.2cm} >{\raggedright\arraybackslash}p{2.0cm}}
        \toprule
        \textbf{Code} & \textbf{Task} & \textbf{h} & \textbf{Dependencies} & \textbf{Resources} \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP0 -- Project management (130\,h, concurrent)}} \\
        \midrule
        T0.1 & Context and scope definition         & 20  & --             & Zotero \\
        T0.2 & Temporal planning                    & 12  & T0.1           & -- \\
        T0.3 & Budget and sustainability            & 15  & T0.2           & -- \\
        T0.4 & Final GEP integration                & 8   & T0.3           & -- \\
        T0.5 & Supervisor meetings                  & 25  & Concurrent     & -- \\
        T0.6 & Thesis writing                       & 50  & Concurrent     & \LaTeX{} \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP1 -- Sprint 1: literature review and baseline (78\,h)}} \\
        \midrule
        T1.1 & OMR literature survey                & 25  & --             & Library access \\
        T1.2 & Dataset exploration                  & 15  & T1.1 (partial) & -- \\
        T1.3 & Classical CV baseline                & 25  & T1.2           & OpenCV \\
        T1.4 & Baseline evaluation                  & 10  & T1.3           & -- \\
        T1.5 & Sprint 1 review                      & 3   & T1.4           & -- \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP2 -- Sprint 2: dataset construction (73\,h)}} \\
        \midrule
        T2.1 & Output encoding (LMX) design         & 12  & T1.5           & music21 \\
        T2.2 & Synthetic rendering pipeline         & 35  & T2.1           & LilyPond \\
        T2.3 & Data augmentation                    & 15  & T2.2           & OpenCV / albumentations \\
        T2.4 & Validation and splits                & 8   & T2.3           & -- \\
        T2.5 & Sprint 2 review                      & 3   & T2.4           & -- \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP3 -- Sprint 3: CRNN--CTC development (75\,h)}} \\
        \midrule
        T3.1 & CRNN architecture design             & 25  & T2.5           & Consumer GPU \\
        T3.2 & Training pipeline                    & 20  & T3.1           & -- \\
        T3.3 & Initial training run                 & 15  & T3.2           & GPU \\
        T3.4 & Evaluation framework                 & 12  & T3.2 (partial) & -- \\
        T3.5 & Sprint 3 review                      & 3   & T3.3, T3.4     & -- \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP4 -- Sprint 4: iterative refinement (68\,h)}} \\
        \midrule
        T4.1 & Error analysis                       & 10  & T3.5           & -- \\
        T4.2 & Data pipeline improvements           & 15  & T4.1           & -- \\
        T4.3 & Model refinement runs                & 25  & T4.2           & GPU \\
        T4.4 & Ablation studies                     & 15  & T4.3           & GPU \\
        T4.5 & Sprint 4 review                      & 3   & T4.4           & -- \\
        \midrule
        \multicolumn{5}{l}{\textbf{WP5 -- Sprint 5: final evaluation and defence (85\,h)}} \\
        \midrule
        T5.1 & Final evaluation                     & 15  & T4.5           & GPU \\
        T5.2 & Qualitative error analysis           & 10  & T5.1           & -- \\
        T5.3 & Extension exploration (conditional)  & 20  & T5.1           & GPU \\
        T5.4 & Final thesis writing                 & 30  & T5.2           & \LaTeX{} \\
        T5.5 & Defence preparation                  & 10  & T5.4           & -- \\
        \midrule
        & \textbf{TOTAL}                            & \textbf{509} & & \\
        \bottomrule
    \end{tabularx}
\end{table}

\section{Earned-Value Budget Control}
\label{app:evm}

This section gives the earned-value-management (EVM) definitions
summarised in \Cref{sec:mgmt-control}.
Each sprint is characterised by three quantities: the
\textit{budgeted cost of work scheduled} (BCWS), the planned cost of
the tasks the sprint should have completed; the \textit{budgeted cost
of work performed} (BCWP), the planned cost of the tasks actually
completed; and the \textit{actual cost of work performed} (ACWP), the
real cost incurred to complete those tasks.
The three control indicators are derived from these primitives.

\textit{Cost variance.}
$\mathrm{CV} = \mathrm{BCWP} - \mathrm{ACWP}$ is the difference
between the budgeted cost of completed work and what it actually
cost.
A positive CV means the sprint came in under budget and a negative
CV means it overran; this is the headline cost-health indicator for
a single sprint.

\textit{Schedule variance.}
$\mathrm{SV} = \mathrm{BCWP} - \mathrm{BCWS}$ is the difference
between the work completed and the work planned, both measured in
the common currency of budgeted cost.
A positive SV means the sprint completed more than was planned and a
negative SV means it fell behind.
SV is reported alongside CV because cost and schedule can move in
opposite directions: a sprint can come in under cost simply by
not completing all its work, which CV alone would not catch.

\textit{Cost performance index.}
$\mathrm{CPI} = \mathrm{BCWP} / \mathrm{ACWP}$ is the
dimensionless ratio of value earned to cost incurred.
A CPI of 1.0 indicates exact agreement with the plan, below 1.0
indicates an overrun, and above 1.0 indicates an underrun.
The advantage of the index form over the raw variance is that it
is directly comparable across sprints of different sizes.

\section{Detailed Budget Derivation}
\label{app:budget-detail}

This section records the per-role rate derivation and the
hand-computed budget arithmetic summarised in
\Cref{sec:mgmt-budget}.

\subsection*{Human-resources rate derivation}

Each task is costed at the loaded rate of the role that
predominantly performs it: the GEP tasks (T0.1--T0.4), the supervisor
meetings (T0.5), and the end-of-sprint reviews (T1.5, T2.5, T3.5,
T4.5) at the Project Manager rate; the literature survey, dataset
exploration, and error-analysis tasks at the Research Analyst rate;
the dataset-rendering, model, and pipeline tasks at the ML Engineer
rate; and the thesis-writing tasks (T0.6, T5.4) together with defence
preparation (T5.5) at the Technical Writer rate.
Aggregating the task hours by role gives \num{92}\,h Project Manager,
\num{72}\,h Research Analyst, \num{255}\,h ML Engineer, and
\num{90}\,h Technical Writer (\num{509}\,h in total), the allocation
shown in \Cref{tab:roles-cost}.
Summing the corresponding costs yields the \EUR{}13\,615.57
human-resources total, which divided by the 509 hours gives a
project-weighted blended rate of \EUR{}26.75/h; this differs slightly
from the unweighted mean of the four rates (\EUR{}26.27/h) because the
ML~Engineer role, the most expensive after the Project Manager,
carries half the project's hours.

\begin{table}[H]
    \centering
    \caption{Roles considered for the project, their loaded hourly
    rates, and the hours each role is charged across the 509-hour
    plan. The role totals reconstruct the human-resources budget of
    \Cref{tab:hr-budget}.}
    \label{tab:roles-cost}
    \small
    \setlength{\tabcolsep}{5pt}
    \renewcommand{\arraystretch}{1.35}
    \begin{tabularx}{\linewidth}{@{} L r r r r r @{}}
        \toprule
        \textbf{Role} & \textbf{Gross (\EUR{}/yr)} & \textbf{Loaded (\EUR{}/yr)} & \textbf{\EUR{}/h} & \textbf{Hours} & \textbf{Cost (\EUR{})} \\
        \midrule
        Project Manager   & 38\,000 & 51\,300 & 29.15 & 92  & 2\,681.80 \\ \tfgrowsep
        Research Analyst  & 33\,000 & 44\,550 & 25.31 & 72  & 1\,822.32 \\ \tfgrowsep
        ML Engineer       & 36\,000 & 48\,600 & 27.61 & 255 & 7\,040.55 \\ \tfgrowsep
        Technical Writer  & 30\,000 & 40\,500 & 23.01 & 90  & 2\,070.90 \\
        \midrule
        \textbf{Total}    &         &         &       & \textbf{509} & \textbf{13\,615.57} \\
        \bottomrule
    \end{tabularx}
\end{table}

The \num{1.35} loading multiplier converts the gross annual salary
used as a public reference into the fully loaded annual cost an
employer actually incurs, and the working year of 1\,760 hours
(220 working days, 8 hours per day, after holidays) follows the FIB
GEP economic-management module.

\subsection*{Hardware amortisation}

The general-costs hardware line is a four-year linear amortisation
prorated for the project duration.
The workstation---a Ryzen~7 7800X3D with 32\,GB of DDR5 memory and an
NVIDIA RTX~3060 12\,GB---is valued at \EUR{}1\,000, the monitor at
\EUR{}200, and the peripherals at \EUR{}45, for a \EUR{}1\,245 base.
Charged linearly over a four-year, \num{208}-week useful life for the
21 project weeks, this gives
$\EUR{}1\,245\times 21/208 \approx \EUR{}125.69$.
